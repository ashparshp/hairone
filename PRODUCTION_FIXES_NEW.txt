================================================================================
HAIRONE — VERIFICATION NOTES (additional / corrections)
================================================================================
Verified against codebase. Items below are NOT in PRODUCTION_FIXES.txt or correct it.

------------------------------------------------------------------------------
CORRECTION — FIX 1 getShopBookings
------------------------------------------------------------------------------
  INVALID to change getShopBookings to activeBooking: true.
  Owner schedule (shop-schedule.tsx) must show completed/cancelled/history.
  Only availability queries need activeBooking: true:
    - bookingController.checkAvailability
    - shopController.getShopSlots
    - shopController.findEarliestSlotForShop
  STATUS: Applied correctly in fix/active-booking-slot-queries.

------------------------------------------------------------------------------
NEW — getTicket returns 200 with null body for bad ID
------------------------------------------------------------------------------
  STATUS: Fixed in fix/support-ticket-ownership (returns 404).

------------------------------------------------------------------------------
NEW — createBooking does not validate serviceNames exist on shop
------------------------------------------------------------------------------
  STATUS: Fixed in fix/server-side-booking-pricing via resolveBookingServices().

------------------------------------------------------------------------------
NEW — bookingKey is 4-digit Math.random with no uniqueness check
------------------------------------------------------------------------------
  STATUS: Fixed in fix/dead-code-cleanup (generateUniqueBookingKey).
  Check-in PIN rate limit added in fix/edge-cases-and-config.

------------------------------------------------------------------------------
NEW — authController JWT sign uses JWT_SECRET without fallback but verify uses fallback
------------------------------------------------------------------------------
  STATUS: Fixed in fix/auth-hardening (centralized getJwtSecret()).

------------------------------------------------------------------------------
REMAINING GAPS — fixed in fix/remaining-gaps
------------------------------------------------------------------------------
  - blockSuspendedOwner on all booking routes (owner APIs when suspended)
  - yearly incident count reset via incidentCountsYear on User
  - no-show/missed count idempotency in updateBookingStatus
  - production CORS fail-fast when CORS_ORIGINS missing
  - final overlap re-check before booking insert inside transaction

------------------------------------------------------------------------------
OPEN (product / infra — not code bugs)
------------------------------------------------------------------------------
  - Real SMS OTP provider for production login (Twilio/Msg91)
  - Cancelled cash bookings counting toward monthly limit (intentional — confirm)
  - Promote main → prod when staging is tested

==============================================================================
ALL FIXES 1–12 IN PRODUCTION_FIXES.txt: COMPLETE
==============================================================================
