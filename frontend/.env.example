# Staging (EAS Android APK / physical phone testing)
EXPO_PUBLIC_API_URL=https://hairone.duckdns.org/api
# Local backend (device must reach your machine — use LAN IP, not 127.0.0.1 on phone)
# EXPO_PUBLIC_API_URL=http://127.0.0.1:8000/api
# Public Razorpay key (same as backend RAZORPAY_KEY_ID) — optional if fetched from /shops/config
EXPO_PUBLIC_RAZORPAY_KEY_ID=rzp_test_xxxxxxxx
